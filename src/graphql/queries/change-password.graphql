
mutation changePassword(
	$username: String!
	$password: String!
	$loginNewPassword: String!
) {
	changePassword(
		username: $username,
		password: $password,
		loginNewPassword: $loginNewPassword
	) {
		lifetime
	}
}
