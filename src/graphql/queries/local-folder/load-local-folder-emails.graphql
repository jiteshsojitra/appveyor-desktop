#import "../../fragments/message.graphql"

mutation loadLocalFolderEmails($accountName: String, $name: String, $dataDirPath: String) {
	loadLocalFolderEmails(
		accountName: $accountName,
		name: $name,
		dataDirPath: $dataDirPath
	) @client {
		...messageFields
	}
}
