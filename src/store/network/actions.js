import { createAction } from 'redux-actions';

export const offlineConnectionStatus = createAction('network offlineConnectionStatus');
export const setSyncInProgress = createAction('network setSyncInProgress');
