import { createAction } from 'redux-actions';

export const showNotificationModal = createAction('notificationModal showNotificationModal');
export const closeNotifyModal = createAction('notificationModal closeNotifyModal');
