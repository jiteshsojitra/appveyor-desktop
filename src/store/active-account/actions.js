import { createAction } from 'redux-actions';
export const setActiveAccountId = createAction('activeAccount set.activeAccountId');
export const setAuthTokenExpired = createAction('activeAccount set authTokenExpired');
