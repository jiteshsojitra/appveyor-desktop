import { createAction } from 'redux-actions';

export const setDragData = createAction('dragdrop setDragData');
export const clearDragData = createAction('dragdrop clearDragData');

export const setDragging = createAction('dragdrop setDragging');
